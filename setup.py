from setuptools import setup, find_packages

setup(
     name = 'src',
     version = '0.0.1',
     author = "Megha",
     author_email= "megha0330@gmail.com",
     packages = find_packages(),
     install_packages = ['pandas','numpy','matplotlib','scipy'],
     )